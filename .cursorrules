## 项目概述
- 这是一个 AI 对话机器人项目，采用 Express 5 后端 + React 19 + Vite 7 前端的 monorepo 架构。
- 后端通过 SSE 流式转发阿里云百炼 DashScope qwen-max 的响应。
- 前端使用 fetch + ReadableStream 消费 SSE 流。

## 目录边界
- 允许修改：server/（后端）、web/src/（前端源码）、web/index.html。
- 禁止修改：node_modules/、dist/、.history/、server/public/（旧版降级入口）。
- 禁止修改 server/.env（敏感凭证）。

## 代码规范
- 使用 `const` 或 `let`，禁止 `var`。
- 统一为有分号风格。
- 统一引号为双引号。
- 优先使用函数式组件和 Hooks。
- 组件使用 React.memo，callback 使用 useCallback。

## 安全红线
- 禁止使用 `dangerouslySetInnerHTML`，除非配合 DOMPurify 使用。
- 禁止将 API 密钥、Token 等硬编码在代码中。
- 禁止使用 `eval()` 或 `new Function()`。
- 前端代码禁止引入后端依赖（express、cors、openai）。

## 输出要求
- 在进行代码审查时，先输出审查计划。
- 按 "严重程度（高/中/低）" 列出问题，并提供修复建议。
- 所有文档和注释使用中文。
