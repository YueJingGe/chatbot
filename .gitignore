# ============================================
# 防御性通配：忽略所有根级 dot 目录
# ============================================
# 无论团队成员安装什么 AI 工具（.cursor/, .claude/, .qoder/, .zed/,
# .junie/, .playwright-mcp/, .agent/, .aider/, .continue/, .codeium/,
# .fleet/, .fig/, .gemini/, .copilot/ 等），
# 其个人状态目录都不会被提交。
# 新增团队共享 dot 目录时记得在下方白名单补一行。
/.*/

# ============================================
# AI 工具特定目录（已被 /.*/ 覆盖，显式声明以明确意图）
# ============================================
# .cursor/       — Cursor 个人状态
# .claude/       — Claude Code 个人状态
# .qoder/        — Qoder 个人状态
# .agents-cache/ — Agent 缓存（非 dot，需额外规则，见下方）

# ============================================
# 显式加回团队共享的 dot 目录
# ============================================
!/.husky/
!/.agents/
!/.vscode/

# ============================================
# .agents/ 白名单：只共享对团队有价值的子目录
# ============================================
# 跟踪（团队共享的 AI 知识库）
!.agents/context/
!.agents/skills/
!.agents/workflow/
!.agents/ignore

# 不跟踪（个人/临时状态）
.agents/archived-changesets/
.agents/docs/
.agents/tmp/

# ============================================
# 依赖
# ============================================
node_modules/

# ============================================
# 环境变量（敏感）
# ============================================
.env
.env.local
.env.*.local

# ============================================
# 构建产物
# ============================================
dist/
build/

# ============================================
# 缓存
# ============================================
.cache/
*.tsbuildinfo

# ============================================
# 系统文件
# ============================================
.DS_Store
Thumbs.db
*.log

# ============================================
# 测试产物
# ============================================
coverage/

# ============================================
# IDE 配置（个人状态）
# ============================================
.idea/

# ============================================
# AI 工具个人产物（非 dot，/.*/ 无法覆盖，需显式忽略）
# ============================================
.agents-cache/
artifacts/
skills-lock.json
