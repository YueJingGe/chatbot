# 完成事项

❗️只是记录，不作为 AI 的参考文档。

## 门禁设计

### 门禁触发

核心路径改动会触发门禁

### 门禁检查证据

把 ledger / simplicity ledger 作为门禁要检查的一种证据。

### ledger 写法

惰性生成：gate 脚本发现缺失时自动弹出模板/提示，让用户（或 AI）现场填写

### 交互位置

pre-push 阶段，由脚本输出 "What to do next" 提示

### 模式

轻量模式仅 warning，strict 模式阻塞
